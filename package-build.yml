apiVersion: kctrl.carvel.dev/v1alpha1
kind: PackageBuild
metadata:
  creationTimestamp: null
  name: kapp-controller.carvel.dev
spec:
  release:
  - resource: {}
  template:
    spec:
      app:
        spec:
          deploy:
          - kapp: {}
          template:
          - ytt:
              paths:
              - ./tmp/release.yml
          - kbld:
              paths:
              - '-'
      export:
      - imgpkgBundle:
          image: ysethiya/test-kctrl
          useKbldImagesLock: true
        includePaths:
        - ./tmp/release.yml
